import { defineComponent } from 'vue';

export default defineComponent({
  name: 'DashboardSelector',
  setup() {
    return () => (
      <div class="p-5 text-center">
        <h3>DashboardSelector Shell</h3>
      </div>
    );
  }
});
